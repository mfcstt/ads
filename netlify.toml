# Documentação: https://docs.netlify.com/configure-builds/overview/

[build]
  command = "npm run build"
  publish = "dist"

# React (Vite): todas as rotas servem o index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
